import SwiftUI

// MARK: - Colors

enum AppColors {
    static let navigationForeground = Color.orange
    static let buttonBorder = Color.orange
    static let buttonText = Color.primary
}

// MARK: - Strings

enum AppStrings {
    static let appTitle = "WEBVIEW LAB"
}

// MARK: - Images

enum AppImages {
    static let appTitleImage = Image("logo.ddd.stamp.1905")
}
